interface:
  display_name: "Codex Hygiene"
  short_description: "Audit Codex context and local activity"
  default_prompt: "Use $codex-hygiene to run the compact read-only Codex context and tool-surface measurement. If I specify a lookback period or retrospective, run the privacy-preserving activity review instead and label evidence, interpretation, and uncertainty."
